#!/bin/bash

ueber_dir=${HOME}/src/ueber
if [ -z "$UEBER_DIR" ]
then
  ueber_dir=$UEBER_DIR
fi

grep --colour -s -nRH "$*" `find $ueber_dir -name "*.rb" -o -name "*.cc" -o -name "*.h" -o -name "*.sh"`
